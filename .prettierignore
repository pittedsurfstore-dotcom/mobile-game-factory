node_modules
.expo
.turbo
dist
build
coverage
pnpm-lock.yaml
.git
*.hbc
# release-please owns CHANGELOG.md and re-writes it with its own formatting;
# excluding it here avoids Format-job churn after every release PR.
CHANGELOG.md
